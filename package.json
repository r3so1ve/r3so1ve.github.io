{
  "name": "securityforge",
  "version": "1.0.0",
  "private": true,
  "description": "Security Forge — static blog built with Eleventy",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "engines": { "node": ">=18" },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
